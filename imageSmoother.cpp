#include "includes.h"

// Easy -- #661
class ImageSmoother {
public:
    static vector<vector<int>> imageSmoother(vector<vector<int>>& img) {
        // matrix is row x col
        int row = img[0].size();
        int col = img.size();
        int j, count;

        for (int i = 0; i < row; i++)
        {
            count = 0;

            
        }


    }

};
